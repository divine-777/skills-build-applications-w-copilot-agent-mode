# Define this to target your backend in GitHub Codespaces.
# The frontend uses import.meta.env.VITE_CODESPACE_NAME
# to build https://<codespace>-8000.app.github.dev/api/... URLs.
VITE_CODESPACE_NAME=your-codespace-name
